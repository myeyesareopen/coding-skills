# Implementation Worker Prompt

Choose the execution tier using [task routing](../SKILL.md). For native Codex tasks use [Codex worker roles](codex-worker.md); for bounded DeepSeek tasks use [the bundled harness protocol](deepseek-dispatch.md). This page retains the previous reference URL without duplicating execution rules.
