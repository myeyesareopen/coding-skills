# Implementation Worker Prompt

Choose the execution tier using [task routing](../SKILL.md), then use [Codex worker roles](codex-worker.md). This page retains the previous reference URL without duplicating execution rules.
