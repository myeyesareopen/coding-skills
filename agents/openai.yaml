interface:
  display_name: "Task Arrangement"
  short_description: "Route code changes with isolation and coordinator integration."
  default_prompt: "Use $task-arrangement to route dependent code changes with isolated workers and coordinator-owned integration."
